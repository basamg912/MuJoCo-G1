<mujoco model="g1_29dof scene">
    <include file="../g1_29dof.xml"/>
    <option timestep="0.002" iterations="50" solver="Newton"/>
    <compiler angle="radian" meshdir="../meshes/"/>

    <visual>
        <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
        <rgba haze="0.15 0.25 0.35 1"/>
        <global azimuth="120" elevation="-20"/>
    </visual>

    <statistic center="0 0 0.5" extent="2.0"/>

    <asset>
        <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
        <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
                markrgb="0.8 0.8 0.8" width="300" height="300"/>
        <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
    </asset>

    <worldbody>
        <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
        <geom name="floor" size="0 0 0.01" type="plane" material="groundplane" group="2"/>

        <!-- Velocity command sliders: ctrl[29]=vx, ctrl[30]=vy, ctrl[31]=wz -->
        <!-- Placed far below scene; high damping keeps joints at rest -->
        <body name="vel_cmd" pos="0 0 -100">
            <inertial mass="1" pos="0 0 0" diaginertia="1 1 1"/>
            <joint name="vx_cmd" type="slide" axis="1 0 0" range="-1 1" damping="10000"/>
            <joint name="vy_cmd" type="slide" axis="0 1 0" range="-1 1" damping="10000"/>
            <joint name="wz_cmd" type="hinge" axis="0 0 1" range="-1 1" damping="10000"/>
            <geom type="sphere" size="0.01" rgba="0 0 0 0" contype="0" conaffinity="0"/>
        </body>
    </worldbody>

    <!-- ctrl[29]=vx, ctrl[30]=vy, ctrl[31]=wz -->
    <actuator>
        <motor name="vx_cmd" joint="vx_cmd" ctrlrange="-1 1" ctrllimited="true"/>
        <motor name="vy_cmd" joint="vy_cmd" ctrlrange="-1 1" ctrllimited="true"/>
        <motor name="wz_cmd" joint="wz_cmd" ctrlrange="-1 1" ctrllimited="true"/>
    </actuator>

</mujoco>
